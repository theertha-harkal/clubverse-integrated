# Base URL of the ClubVerse Spring Boot backend.
# Copy this file to .env (or .env.local) and adjust if your backend runs
# somewhere other than the default localhost:8080.
VITE_API_BASE_URL=http://localhost:8080
